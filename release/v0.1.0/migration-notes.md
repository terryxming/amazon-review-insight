# Migration Notes

v0.1.0 is the first public skill release.

No migration is required from an earlier version. Future releases must document:

1. data contract changes,
2. report contract changes,
3. Sorftime field changes,
4. CLI command changes,
5. eval dataset changes,
6. lifecycle status changes.

